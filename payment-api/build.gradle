tasks.named('bootJar') {
    enabled = true
}

tasks.named('jar') {
    enabled = false
}

dependencies {

    implementation project(':inventory-api')
    implementation 'org.springframework.boot:spring-boot-starter-web'
}