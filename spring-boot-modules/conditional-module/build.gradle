plugins {
    id 'lombok-convention'
}

tasks.named('bootJar') {
    enabled = false
}

tasks.named('jar') {
    enabled = true
}

group = 'memory.library'
version = 'auto-config-SNAPSHOT'

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter-web'
}
